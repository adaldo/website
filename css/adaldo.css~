/* Main style */
body {
  font-family: "ubuntu", verdana, sans-serif;
  font-size: 18px;
  text-align: left;
  margin-left: 2em;
  margin-right: 2em;
  background-color: #333;
  color: #eee;
}

aside {
  background-color: #888;
}

/* For potrait pictures */
.face {
  border-radius: 50%;
  border-style: solid;
  border-width: 0.25em;
  border-color: #eee;
  max-width: 12em;
  display: inline-block;
  margin: 2em;
}



/* Heading margins */
h2, h3 {
  margin-top: 2em;
}

/* unvisited link */
a:link {
    color: lightblue;
}

/* visited link */
a:visited {
    color: lightpink;
}

/* selected link */
a:active {
    color: lightgreen;
}



/* For time */
time {
  color: lightcoral;
}



/* Navigation bar */
nav > ul {
    list-style-type: none;
    margin: 0;
    padding: 0;
    overflow: hidden;
}

nav > ul > li {
    float: left;
}

nav > ul > li > a {
  display: block;
  color: #eee;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

nav > ul > li > a:hover {
  background-color: steelblue;
  border-radius: 0.5em;
  color: #eee;
}



/* For calendar icons */

time.icon
{
  font-size: 0.75em; /* change icon size */
  display: block;
  position: relative;
  width: 7em;
  height: 7em;
  background-color: #eee;
  border-radius: 0.6em;
  overflow: hidden;
}

time.icon *
{
  display: block;
  width: 100%;
  font-size: 1em;
  font-weight: bold;
  font-style: normal;
  text-align: center;
}

time.icon strong
{
  position: absolute;
  top: 0;
  padding: 0.4em 0;
  color: #eee;
  background-color: darkorange; /*#ff0000*/;
  border-bottom: 1px coral /*#990000*/;
  /*box-shadow: 0 2px 0 #fd9f1b #ff0000;*/
}

time.icon em
{
  position: absolute;
  bottom: 0.3em;
  color: darkorange /*#008000*/;
}

time.icon span
{
  font-size: 2.8em;
  letter-spacing: -0.05em;
  padding-top: 0.8em;
  color: #2f2f2f;
}

td.newsdate {
  margin: 2em;
}

td.newsitem {
  margin: 2em;
  padding: 2em;
}


/* Footer */
footer {
  margin-top: 3em;
  padding-top: 1em;
  padding-bottom: 1em;
  border-top-style: solid;
  border-top-width: 3px;
}
